---
"@ampless/admin": minor
"create-ampless": minor
---

blog theme: add a permalink + X share footer to the post page, and cap the home-feed tags at 3 per post (with a "+N" overflow indicator). Adds `public.permalink` / `public.shareOnX` locale strings (en/ja) and an optional `max` prop on the shared `TagList` component.
